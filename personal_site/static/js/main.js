$(function () {
  // enable toggle popovers
  $('.popover-toggle').popover({
    container: 'body'
  });
  // enable dismiss popovers
  $('.popover-dismiss').popover({
    trigger: 'focus',
    container: 'body'
  });
})

